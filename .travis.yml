language: python
python: 2.7
cache:
 directories:
  - $HOME/miniconda3

before_install:
 - pip install --upgrade pip setuptools
 - pip install planemo
 
script:
- planemo lint ${TRAVIS_BUILD_DIR}/tools/msPurity 
- travis_wait 20 planemo test  --conda_debug  --conda_ensure_channels tomnl,iuc,bioconda,conda-forge,defaults  ${TRAVIS_BUILD_DIR}/tools/msPurity/spectralMatching.xml  

