language: python

before_install:
 - pip install --upgrade pip setuptools
 - pip install planemo
script:
- planemo lint ${TRAVIS_BUILD_DIR}/tools/ 
- planemo test  --conda_ensure_channels computational-metabolomics,tomnl,iuc,bioconda,conda-forge,defaults --no_cache_galaxy ${TRAVIS_BUILD_DIR}/tools/  
