<tool id="assess_purity_msms" name="assess_purity_msms" version="0.0.9">
    <description>
        Assess the precursor ion purity for fragmentation spectra acquired from either a LC-MS/MS or DI-MS/MS (MSn) experiment
    </description>
    <macros>
        <import>macros.xml</import>
    </macros>

    <expand macro="requirements" />

    <stdio>
        <exit_code range="1:" />
    </stdio>

    <command><![CDATA[
        Rscript $__tool_directory__/assess_purity_msms.R
            
            --out_dir=.
            
        
            #set mzML_files = '|'.join( [ str( $single_file ) for $single_file in $filesource ] )
            #set FileNames = '|'.join( [ str( $single_file.name ) for $single_file in $filesource ] )
                --singlefile_galaxyPath='$mzML_files'
                --singlefile_sampleName='$FileNames'


            #if $offsets.offsets == 'user'
                --minOffset=$minoffset
                --maxOffset=$maxoffset
            #end if


            --iwNorm=$iw_norm
            --ilim=$ilim


            --cores=\${GALAXY_SLOTS:-4}


            #if $nearest
                --nearest
            #end if


            #if $mostIntense
                --mostIntense
            #end if


            #if $isotopes.isotopes == "exclude_default"
                --exclude_isotopes
            #elif $isotopes.isotopes == "user"
                --exclude_isotopes
                --isotope_matrix=$isotopes.im
            #end if


    ]]></command>

    <inputs>

        <!-- Choose if your input has MS and MSn datas in the same file or not -->
        <param name="filesource" type="data" format="mzml" label="*.mzML files" multiple="true">
            <validator type="empty_field"/>
        </param>  
        
        <!-- Choose how determine the precursor -->
        <param name="mostIntense" type="boolean" checked="true" label="Use most intense peak within isolation window for precursor?" help="If yes, this will ignore the recorded precursor within the mzML file and use the most intense peak within the isolation window to calculate the precursor ion purity"/>
        <param name="nearest" type="boolean" checked="true" label="Use nearest full scan to determine precursor?" help="If yes, this will use the neareset full scan to the fragmentation scan to determine what the m/z value is of the precursor"/>

        <!-- -->
        <conditional name="offsets">
            <param name="offsets" type="select" label="offsets" >
                <option value="auto" selected="true">Uses offsets determined in the mzML file</option>
                <option value="user">User supplied offset values</option>
            </param>
            <when value="user">
                <expand macro="offsets" />
            </when>
            <when value="auto">
            </when>
        </conditional>

        <!-- Parameters ilim, normalisation for isolation efficiency and the handling of isotopic peaks -->
        <expand macro="general_params" />

    </inputs>

    <outputs>

        <data name="assess_purity" format="tsv" label="${tool.name}_on_${'|'.join( [ str( $single_file.name ) for $single_file in $filesource ] )}: tsv" from_work_dir="purity_msms.tsv">  
        </data>

        <data name="assess_purity_onefile_rdata" format="rdata" label="${tool.name}_on_${'|'.join( [ str( $single_file.name ) for $single_file in $filesource ] )}: RData" from_work_dir="purity_msms.RData">
        </data>

    </outputs>

    <tests>

        <test>
            <param name="filesource" value="LCMSMS_1.mzML"/>
            <output name="assess_purity_onefile" value="assess_purity_msms_on_LCMSMS_1.tsv"/>
        </test>

    </tests>
    
    <help><![CDATA[
=============================================================
Assess precursor ion purity of MS/MS files
=============================================================
-----------
Description
-----------

Tool to assess precursor ion purity of MS/MS files

Data input of mzML files either from:

* A data collection of the mzML files containing MS/MS scans
* A path to a folder that has mzML files containing MS/MS scans

See Bioconductor documentation for more details about the function used, msPurity::purityA().

-----------
Outputs
-----------

* purity_msms: A tsv file of all the precursor ion purity score (and other metrics) of each fragmentation spectra
* purity_msms_rdata: The purityA object saved as an rdata file


    ]]></help>
<expand macro="citations" />
</tool>
