<tool id="track_rt_raw" name="track_rt_raw" version="0.0.1">

    <requirements>
            <requirement type="package" >R</requirement>
	        <requirement type="package" >r-optparse</requirement>
    </requirements>

    <description>Track the raw retention time in the peak slot of XCMSset objects.
    </description>
    <stdio>
        <exit_code range="1:" />
    </stdio>
    <command interpreter="Rscript"><![CDATA[
        track_rt_raw.R
            --xset_path=$xset
            --out_dir=.

    ]]></command>
    <inputs>

        <param type="data" name="xset"
                help="xcmsSet object saved as an RData file"/>

    </inputs>
    <outputs>

        <data name="xset_with_rt_raw" format="rdata" label="${tool.name} on ${on_string}: RData"
              from_work_dir="xset_rt_raw_tracked.RData" visible="true"/>
    </outputs>
    <tests>

    </tests>
    <help><![CDATA[]]></help>
</tool>